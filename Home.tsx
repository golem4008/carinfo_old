# 数据说明文档

本目录包含项目中使用的所有模拟数据。如果您想添加或修改数据，请按照以下说明操作：

## 数据文件位置

所有数据都存放在 `src/mocks/data.ts` 文件中。

## 如何添加新数据

### 1. 添加新的车企

在 `carCompanies` 数组中添加新的车企对象：

```typescript
{ 
  id: '唯一标识符', 
  name: '车企名称', 
  logo: 'logo图片URL', 
  color: '用于图表的颜色' 
}
```

### 2. 添加月度销量数据

在对应年份的数据数组中添加新的销量记录（如 `monthlySalesData2025`）：

```typescript
{ 
  month: '月份', // 如 '1月', '2月' 等
  sales: 销量数值, // 如 10000
  company: '车企名称', // 必须与 carCompanies 中的名称一致
  year: 年份 // 如 2025
}
```

### 3. 更新市场份额数据

在 `marketShareData` 数组中添加或更新对应车企的市场份额：

```typescript
{ 
  company: '车企名称', 
  share: 市场份额百分比, // 如 25 表示 25%
  color: '用于图表的颜色' // 建议与 carCompanies 中的颜色一致
}
```

## 数据说明

- 所有数据都基于 TypeScript 接口进行类型约束
- 增长率数据是通过 `getGrowthRateData()` 函数根据月度销量数据计算得出的
- 销量趋势数据是通过 `getSalesTrendData()` 函数格式化月度销量数据得出的
- 总销量、平均销量和同比增长等指标都是通过相应的函数计算得出的

## 注意事项

- 添加新数据时，请确保保持数据格式的一致性
- 对于新添加的车企，建议同时添加其月度销量数据和市场份额数据
- 如果修改了历史数据，相关的计算指标也会自动更新